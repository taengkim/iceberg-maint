# Python packages installed into the Spark Docker image.
# Keep this minimal — heavy dependencies increase image size and build time.

# PyYAML is used by the PySpark job for any YAML-based config (if needed).
pyyaml>=6.0.2,<7

# boto3 is available for S3 operations outside of Spark (e.g., listing files).
# The Spark job itself uses the JVM-based hadoop-aws / AWS SDK for S3 access.
boto3>=1.34,<2
